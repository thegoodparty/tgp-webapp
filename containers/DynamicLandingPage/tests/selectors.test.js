// import { selectDynamicLandingPageDomain } from '../selectors';

describe('selectDynamicLandingPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
