// import { selectSnackbarContainerDomain } from '../selectors';

describe('selectSnackbarContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
