// import { selectRegisterComboContainerDomain } from '../selectors';

describe('selectRegisterComboContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
