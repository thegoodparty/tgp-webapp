// import { selectAdminCandidateListPageDomain } from '../selectors';

describe('selectAdminCandidateListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
