// import { selectAdminAddCandidatePageDomain } from '../selectors';

describe('selectAdminAddCandidatePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
