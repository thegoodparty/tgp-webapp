// import { selectAdminCandidateApplicationsPageDomain } from '../selectors';

describe('selectAdminCandidateApplicationsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
