/**
 * Test sagas
 */

/* eslint-disable redux-saga/yield-effects */
// import { take, call, put, select } from 'redux-saga/effects';
// import adminCandidateApplicationsPageSaga from '../saga';

// const generator = adminCandidateApplicationsPageSaga();

describe('adminCandidateApplicationsPageSaga Saga', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
