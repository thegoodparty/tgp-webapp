// import { selectCreatorsPageDomain } from '../selectors';

describe('selectCreatorsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
