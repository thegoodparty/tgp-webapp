// import { selectPortalAdminPageDomain } from '../selectors';

describe('selectPortalAdminPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
