// import { selectStaffManagementPageDomain } from '../selectors';

describe('selectStaffManagementPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
