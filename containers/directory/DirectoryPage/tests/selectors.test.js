// import { selectDirectoryPageDomain } from '../selectors';

describe('selectDirectoryPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
