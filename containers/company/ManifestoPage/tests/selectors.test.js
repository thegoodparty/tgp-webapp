// import { selectManifestoPageDomain } from '../selectors';

describe('selectManifestoPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
