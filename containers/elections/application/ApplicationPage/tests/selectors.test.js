// import { selectApplicationPageDomain } from '../selectors';

describe('selectApplicationPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
