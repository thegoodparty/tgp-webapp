// import { selectCandidatePageDomain } from '../selectors';

describe('selectCandidatePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
