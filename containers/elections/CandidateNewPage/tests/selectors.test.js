// import { selectCandidateNewPageDomain } from '../selectors';

describe('selectCandidateNewPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
