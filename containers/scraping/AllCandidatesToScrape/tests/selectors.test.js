// import { selectAllCandidatesToScrapeDomain } from '../selectors';

describe('selectAllCandidatesToScrapeDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
