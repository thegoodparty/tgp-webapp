// import { selectTwitterCallbackPageDomain } from '../selectors';

describe('selectTwitterCallbackPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
