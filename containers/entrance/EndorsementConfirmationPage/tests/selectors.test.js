// import { selectEndorsementConfirmationPageDomain } from '../selectors';

describe('selectEndorsementConfirmationPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
