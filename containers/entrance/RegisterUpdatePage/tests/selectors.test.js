// import { selectRegisterUpdatePageDomain } from '../selectors';

describe('selectRegisterUpdatePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
