// import { selectLoginPasswordPageDomain } from '../selectors';

describe('selectLoginPasswordPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
