// import { selectVoterizePageDomain } from '../selectors';

describe('selectVoterizePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
