// import { selectIncumbentsToScrapeDomain } from '../selectors';

describe('selectIncumbentsToScrapeDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
